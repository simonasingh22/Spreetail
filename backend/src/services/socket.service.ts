export const broadcastToGroup = (groupId: string, event: string, payload: any) => {
  // socket service placeholder
};
export const broadcastToExpense = (expenseId: string, event: string, payload: any) => {
  // socket service placeholder
};
